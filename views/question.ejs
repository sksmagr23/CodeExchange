<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Question</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 text-gray-900">
  <header class="bg-blue-500 text-white p-4">
    <div class="container mx-auto">
      <h1 class="text-xl font-bold">CodeExchange</h1>
      <nav>
        <a href="/" class="mr-4">Home</a>
        <% if (user) { %>
          <a href="/questions" class="mr-4">Questions</a>
          <a href="/profile" class="mr-4">Profile</a>
          <a href="/api/auth/logout" class="mr-4">Logout</a>
        <% } else { %>
          <a href="/login" class="mr-4">Login</a>
          <a href="/register" class="mr-4">Register</a>
        <% } %>
      </nav>
    </div>
  </header>
  
  <main class="container mx-auto mt-4">
    <div class="bg-white p-6 rounded-lg shadow-lg">
      <h1 class="text-2xl font-bold mb-4"><%= question.title %></h1>
      <p class="text-gray-700 mb-4"><%= question.description %></p>
      <p class="text-sm text-gray-500 mb-4">posted by <%= question.user.username %> on <%= new Date(question.createdAt).toDateString() %></p>

      <h2 class="text-xl font-semibold mb-4">Answers</h2>
      <% answers.forEach(answer => { %>
        <div class="mb-4">
          <p class="text-gray-700"><%= answer.text %></p>
          <p class="text-sm text-gray-500">- by <%= answer.user.username %> on <%= new Date(answer.createdAt).toDateString() %></p>
        </div>
      <% }) %>

      <% if (user) { %>
        <form action="/questions/<%= question._id %>/answers" method="POST">
          <div class="mb-4">
            <label for="text" class="block text-gray-700">Your Answer</label>
            <textarea name="text" id="text" class="w-full px-3 py-2 border rounded" placeholder="If you know an answer to this question, you can answer it or can left a suggestion"></textarea>
          </div>
          <div class="mb-4">
            <button type="submit" class="w-full px-3 py-2 bg-blue-500 text-white rounded">Post Answer</button>
          </div>
        </form>
      <% } else { %>
        <p class="text-gray-700">Please <a href="/login" class="text-blue-500">login</a>/<a href="/register" class="text-blue-500">register</a> to post an answer.</p>
      <% } %>
    </div>
  </main>
</body>
</html>
