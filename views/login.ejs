<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="icon" type="image/svg+xml" href="/fav.svg">
  <title>Login - CodeExchange</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&family=Fira+Code:wght@400;500;600&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/styles.css">
  <script>
    tailwind.config = {
      theme: {
        extend: {
          fontFamily: {
            sans: ['Poppins', 'sans-serif'],
            mono: ['Fira Code', 'monospace'],
          }
        }
      }
    }
  </script>
</head>
<body class="bg-gradient-to-br from-[#0466C8] to-[#0353A4] text-gray-800 font-sans min-h-screen flex flex-col">
  <header class="bg-[#0466C8] text-white shadow-lg sticky top-0 z-50">
    <div class="container mx-auto px-4 py-3">
      <div class="flex justify-between items-center">
        <div class="flex items-center">
          <i class="fas fa-code text-xl mr-2"></i>
          <h1 class="text-2xl font-bold font-mono">CodeExchange</h1>
        </div>
        <nav class="flex space-x-6">
          <a href="/" class="nav-link">
            <i class="fas fa-home mr-1"></i> Home
          </a>
          <a href="/register" class="nav-link">
              <i class="fas fa-user-plus mr-1"></i>
              Register
          </a>
        </nav>
      </div>
    </div>
  </header>
  
  <main class="flex-grow flex items-center justify-center py-12 px-4">
    <div class="bg-white rounded-xl shadow-lg w-full max-w-md p-8">
      <div class="text-center mb-8">
        <div class="inline-flex items-center justify-center w-16 h-16 bg-blue-100 rounded-full mb-4">
          <i class="fas fa-user-circle text-[#0466C8] text-3xl"></i>
        </div>
        <h1 class="text-2xl font-bold text-[#023E7D]">Welcome Back</h1>
        <p class="text-gray-600 mt-2">Sign in to your CodeExchange account</p>
      </div>

      <form action="/api/auth/login" method="POST" class="space-y-5">
        <div>
          <label for="email" class="block text-sm font-medium text-gray-700 mb-1">Email Address</label>
          <div class="relative">
            <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
              <i class="fas fa-envelope text-gray-400"></i>
            </div>
            <input type="email" name="email" id="email" 
                  class="block w-full pl-10 pr-3 py-2.5 border border-gray-300 rounded-lg focus:ring-2 focus:ring-[#0466C8] focus:border-[#0466C8] shadow-sm outline-none" 
                  placeholder="your@email.com" required>
          </div>
        </div>
        
        <div>
          <div class="flex items-center justify-between mb-1">
            <label for="password" class="block text-sm font-medium text-gray-700">Password</label>
          </div>
          <div class="relative">
            <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
              <i class="fas fa-lock text-gray-400"></i>
            </div>
            <input type="password" name="password" id="password" 
                  class="block w-full pl-10 pr-3 py-2.5 border border-gray-300 rounded-lg focus:ring-2 focus:ring-[#0466C8] focus:border-[#0466C8] shadow-sm outline-none" 
                  placeholder="••••••••" required>
          </div>
        </div>

        <button type="submit" 
                class="w-full bg-[#0466C8] text-white py-2.5 px-4 rounded-lg hover:bg-[#0353A4] transition shadow-md focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-[#0466C8]">
          Sign in
        </button>
      </form>

      <div class="flex items-center my-6">
        <div class="flex-grow border-t border-gray-300"></div>
        <span class="px-4 text-gray-500 text-sm">OR</span>
        <div class="flex-grow border-t border-gray-300"></div>
      </div>

      <div>
        <a href="/api/auth/google" 
           class="w-full flex items-center justify-center space-x-3 bg-white border border-gray-300 text-gray-700 py-2.5 px-4 rounded-lg hover:bg-gray-50 transition shadow-sm">
          <svg class="w-5 h-5" viewBox="0 0 24 24">
            <path fill="#4285F4" d="M22.56 12.25c0-.78-.07-1.53-.2-2.25H12v4.26h5.92c-.26 1.37-1.04 2.53-2.21 3.31v2.77h3.57c2.08-1.92 3.28-4.74 3.28-8.09z"/>
            <path fill="#34A853" d="M12 23c2.97 0 5.46-.98 7.28-2.66l-3.57-2.77c-.98.66-2.23 1.06-3.71 1.06-2.86 0-5.29-1.93-6.16-4.53H2.18v2.84C3.99 20.53 7.7 23 12 23z"/>
            <path fill="#FBBC05" d="M5.84 14.09c-.22-.66-.35-1.36-.35-2.09s.13-1.43.35-2.09V7.07H2.18C1.43 8.55 1 10.22 1 12s.43 3.45 1.18 4.93l2.85-2.22.81-.62z"/>
            <path fill="#EA4335" d="M12 5.38c1.62 0 3.06.56 4.21 1.64l3.15-3.15C17.45 2.09 14.97 1 12 1 7.7 1 3.99 3.47 2.18 7.07l3.66 2.84c.87-2.6 3.3-4.53 6.16-4.53z"/>
          </svg>
          <span class="font-medium">Continue with Google</span>
        </a>
      </div>
      
      <p class="text-center mt-8 text-gray-600">
        Don't have an account? <a href="/register" class="text-[#0466C8] hover:text-[#0353A4] font-medium">Register now</a>
      </p>
    </div>
  </main>

  <footer class="bg-[#023E7D] py-6 text-white">
    <div class="container mx-auto px-6">
      <div class="flex flex-col md:flex-row justify-between items-center">
        <div class="mb-4 md:mb-0 flex items-center">
          <i class="fas fa-code text-xl mr-2"></i>
          <span class="font-mono font-bold text-lg">CodeExchange</span>
        </div>
        <div class="mb-4 md:mb-0 text-sm text-gray-200">
          &copy; 2024 CodeExchange. All rights reserved. Created by Saksham
        </div>
      </div>
    </div>
  </footer>
  <div id="toast" class="fixed bottom-4 right-4 max-w-xs bg-white border-l-4 border-red-500 shadow-lg rounded-lg p-4 transform translate-x-full transition-transform duration-300 ease-in-out opacity-0">
  <div class="flex items-center">
    <div class="flex-shrink-0 text-red-500">
      <i class="fas fa-exclamation-circle"></i>
    </div>
    <div class="ml-3">
      <p id="toast-text" class="text-sm text-gray-800 font-medium"></p>
    </div>
    <div class="ml-auto pl-3">
      <button id="close-toast" class="text-gray-400 hover:text-gray-900">
        <i class="fas fa-times"></i>
      </button>
    </div>
  </div>
</div>

  <style>
    .nav-link {
      @apply px-3 py-2 rounded-md hover:bg-white hover:bg-opacity-10 transition;
    }
    .nav-link.active {
      @apply bg-white bg-opacity-10;
    }
  </style>
  <script>
  const urlParams = new URLSearchParams(window.location.search);
  const error = urlParams.get('error');
  
  if (error) {
    const toast = document.getElementById('toast');
    const toastText = document.getElementById('toast-text');
    
    if (error === 'invalid_credentials') {
      toastText.textContent = 'Invalid email or password. Please try again.';
    } else if (error === 'oauth_error') {
      toastText.textContent = 'An error occurred during authentication.';
    } else if (error === 'oauth_failed') {
      toastText.textContent = 'Authentication failed.';
    }
    
    toast.classList.remove('translate-x-full', 'opacity-0');
    
    setTimeout(() => {
      toast.classList.add('translate-x-full', 'opacity-0');
    }, 5000);
    
    document.getElementById('close-toast').addEventListener('click', () => {
      toast.classList.add('translate-x-full', 'opacity-0');
    });
  }
</script>
</body>
</html>
